--  18/5/26       alter table
alter table std_record add std_phone varchar(30);

alter table std_record add std_email varchar(30), add std_address varchar(30);

alter table std_record drop column std_address;

alter table std_record add std_country varchar(30) default "India";

alter table std_record rename column std_country to std_location;

select * from std_record;

truncate table std_record;

show tables from 23bce1200;

create table temp(
name int1
);

delete from temp;

rename table std_record to student_record;

----------

CREATE TABLE Worker (
	WORKER_ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
	FIRST_NAME CHAR(25),
	LAST_NAME CHAR(25),
	SALARY INT(15),
	JOINING_DATE DATETIME,
	DEPARTMENT CHAR(25)
);

INSERT INTO Worker 
	(WORKER_ID, FIRST_NAME, LAST_NAME, SALARY, JOINING_DATE, DEPARTMENT) VALUES
		(001, 'Monika', 'Arora', 100000, '14-02-20 09.00.00', 'HR'),
		(002, 'Niharika', 'Verma', 80000, '14-06-11 09.00.00', 'Admin'),
		(003, 'Vishal', 'Singhal', 300000, '14-02-20 09.00.00', 'HR'),
		(004, 'Amitabh', 'Singh', 500000, '14-02-20 09.00.00', 'Admin'),
		(005, 'Vivek', 'Bhati', 500000, '14-06-11 09.00.00', 'Admin'),
		(006, 'Vipul', 'Diwan', 200000, '14-06-11 09.00.00', 'Account'),
		(007, 'Satish', 'Kumar', 75000, '14-01-20 09.00.00', 'Account'),
		(008, 'Geetika', 'Chauhan', 90000, '14-04-11 09.00.00', 'Admin');
        
select * from Worker;

update worker set first_name="gowshick", last_name="s" where worker_id=1;

update worker set salary=salary+5000;

delete from worker where worker_id=6;

start transaction;

create table emp(id int);

insert into emp values (101);

savepoint time1;

select * from emp;

insert into emp values(102);

savepoint time2;
select * from emp;
insert into emp values(103);
savepoint time3;

insert into emp values(104);
savepoint time4;
Rollback to time3;

drop table emp;


CREATE TABLE emp(id int);

START TRANSACTION;

INSERT INTO emp VALUES (101);

SAVEPOINT time1;

INSERT INTO emp VALUES (102);

SAVEPOINT time2;

INSERT INTO emp VALUES (103);

SAVEPOINT time3;

INSERT INTO emp VALUES (104);

SAVEPOINT time4;

ROLLBACK TO time3;

SELECT * FROM emp;

commit;
select * from emp;

show tables from 23bce1200;
